namespace ParkingControl.Portal.Constants
{
    public static class ConfigurationConstants
    {
        public const string BlobStorage = nameof(BlobStorage);
        public const string ConnectionString = nameof(ConnectionString);
    }
}
